services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "${HTTP_PORT:-8080}:${HTTP_PORT:-8080}"
    volumes:
      - .:/app
    env_file:
      - .env
    stdin_open: true
    tty: true
